import React from "react";

// Use React Routers for switching between the different quality versions of the video stream

const VideoPlayer = () => {
    return (
        <div>
            Video
        </div>
    )
}

export default VideoPlayer;