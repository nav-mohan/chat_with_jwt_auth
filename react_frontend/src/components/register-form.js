import React from "react";
import { Form } from "react-bootstrap";
import { nodeBaseUrl, registerPath } from "../config";

const RegisterButton = ({
    email,password, user_login, user_nicename, 
    user_url, display_name, nickname, first_name,
    last_name, description
}) => {
    return (
        <Form className="">
            <Form.Control>
                
            </Form.Control>
        </Form>
    )
}